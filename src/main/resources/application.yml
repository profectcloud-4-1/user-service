spring:
  application:
    name: "user-service"
  profiles:
    active: ${USER_PROFILE:dev}

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never

logging:
  level:
    root: INFO
